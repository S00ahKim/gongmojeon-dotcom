table.info_table
  tr 
    th 분야
    td.smallText=comp_info.topic
    th 주최사
    td.smallText=comp_info.host
    th 대상
    td.smallText=comp_info.applicant

  tr  
    th 담당자
    td.smallText=comp_info.manager
    th 연락처
    td.smallText=comp_info.contact
    th 장소
    td.smallText=comp_info.location

  tr
    th 시작일자
    td.smallText=comp_info.start
    th 마감일자
    td.smallText=comp_info.end

if comp_info.location_map
  .smallText!= comp_info.location_map
  .smallText#lat(data-value=comp_info.lat)= comp_info.lat 
  .smallText#lng(data-value=comp_info.lng)= comp_info.lng
  .map_wrap
    #map(style='width:100%;height:100%;position:relative;overflow:hidden;')
      |      
    .hAddr        
      span#centerAddr
        script(type='text/javascript', src='//dapi.kakao.com/v2/maps/sdk.js?appkey=7235e7c57191d55774b383b2856384b2&libraries=services')
        script.
          var mapContainer = document.getElementById('map'), // 지도를 표시할 div 
              mapOption = { 
                  center: new daum.maps.LatLng($('#lat').data('value'), $('#lng').data('value')), // 지도의 중심좌표
                  level: 3 // 지도의 확대 레벨
              };

          var staticmap = new daum.maps.StaticMap(mapContainer, mapOption); // 지도를 생성합니다

          // 지도를 클릭한 위치에 표출할 마커입니다
          var marker = new daum.maps.Marker({ 
              // 지도 중심좌표에 마커를 생성합니다 
              position: markerPosition             
          }); 
              // 지도에 마커를 표시합니다
          marker.setMap(map);
          map.setDraggable(false);
          map.setZoomable(false); 


.content !{comp_info.content}


br
br
p(style= "font-weight: bold") 참고
.content=comp_info.ref

.tags.badges
  each tag in comp_info.tags 
    span.badge.badge-phill.badge-secondary= tag
    | 
.row
  .col-sm
    .numbers.
      #[span.number.num-votes= comp_info.numLikes] 좋아요,
      #[span.number.num-comments= comp_info.numComments] 댓글,
      #[span.number.num-views= comp_info.numReads] 조회
  .col-sm
    .author
      | 글작성시간 #{moment(comp_info.createdAt).format('YYYY-MM-DD')}
      if comp_info.author
        | 
        | by #[a(href=`/users/${comp_info.author.id}`)= comp_info.author.name] 
