extends ../layout

block content
  .container
    h1.main-title 승인 대기 중인 글
          
    br
    if compInfos
      table.table.table-hover
        thead
          tr
            th 제목
            th 주최사
            th 연락처
            th 
        tbody
            each a in compInfos
              tr
                td
                  a(href=`/compInfos/${a._id}`)= a.title
                td= a.host
                td= a.contact
                td
                  a(href=`/compInfos/${a._id}/upload`) 승인하기
    else
        p 승인 대기 중인 글이 없습니다.